[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npx prisma generate --schema=prisma/schema.prisma"
]

[start]
cmd = "npx prisma db push --accept-data-loss --skip-generate --schema=prisma/schema.prisma && node src/server.js"
